import { SectionHeader } from '@/components/ui/SectionHeader';

export default function StockLedgerPlaceholder() {
  return (
    <div className="space-y-6">
      <SectionHeader title="Stock Ledger" description="Stock Ledger report coming in next phase." />
    </div>
  );
}
