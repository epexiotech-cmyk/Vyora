import { SectionHeader } from '@/components/ui/SectionHeader';

export default function MovementRegisterPlaceholder() {
  return (
    <div className="space-y-6">
      <SectionHeader
        title="Stock Movement Register"
        description="Movement Register report coming in next phase."
      />
    </div>
  );
}
